// Explorer's chart palette, authored in sRGB.
const vec3 SUN_DIR = vec3(-0.55, 0.75, -0.40);
const vec3 SEPIA = vec3(0.27, 0.18, 0.10);
const vec3 WAVE_INK = vec3(0.24, 0.37, 0.44);
const vec3 SHALLOWS = vec3(0.76, 0.86, 0.86);
const vec3 DEEP_WATER = vec3(0.50, 0.66, 0.73);
const vec3 FOAM = vec3(0.95, 0.94, 0.88);
const vec3 STONE = vec3(0.68, 0.55, 0.40);
const vec3 STONE_LIGHT = vec3(0.88, 0.79, 0.62);
const vec3 CANOPY_DARK = vec3(0.30, 0.37, 0.19);
const vec3 SAND = vec3(0.92, 0.85, 0.66);
const vec3 PAPER = vec3(0.89, 0.82, 0.67);
// Scorched edge colour, relative to the local paper brightness.
const vec3 EDGE_TINT = vec3(0.78, 0.66, 0.50);
